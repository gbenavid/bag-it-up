import React, { Component, PropTypes } from 'react';

class Item extends Component {
  constructor(props) {
    super(props);
  }
  render () {
    return (
      <section>          
          "Hello, World".
      </section>
    );
  }
}

export default Item;
